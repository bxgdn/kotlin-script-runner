// Error Example
// This script intentionally contains an error
// to demonstrate clickable error locations

println("This line works fine")
println("So does this one")

// This will cause a compilation error
undefinedFunction()

println("This line won't be reached")

